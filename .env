# URL del backend Flask
# In sviluppo locale lascia vuoto (usa il proxy di Vite su /api)
# Su un altro PC o server imposta l'IP reale del backend, es:
#   VITE_API_URL=http://192.168.1.50:5000/api
VITE_API_URL=http://127.0.0.1:5000/api
